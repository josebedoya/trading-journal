/**
 * Drizzle schema — single source of truth del modelo de datos.
 *
 * Fase 0 (scaffold): vacío a propósito. Las tablas del modelo
 * (users, accounts, trades, transactions, setups, tags, screenshots,
 * daily_prep, journal_entries, pre_trade_evaluations) se definen en la
 * Fase 1 — ver §4 de CLAUDE.md.
 *
 * Reglas: dinero en `numeric` (nunca float), timestamps `withTimezone` en UTC.
 */

export {};
